# Copiar este archivo a .env.local (para desarrollo) y NUNCA commitear el real.
# En producción, estas mismas variables se configuran en Vercel:
# Settings -> Environment Variables (aplicadas al entorno Production).

# API key de Groq (inferencia openai/gpt-oss-120b): https://console.groq.com/keys
GROQ_API_KEY=

# API key de Pinecone: https://app.pinecone.io/ -> API Keys
PINECONE_API_KEY=

# Host del índice de Pinecone (NO es el mismo para todos los índices -- cada
# índice tiene su propio host único). Para conseguirlo:
#   python -c "from pinecone import Pinecone; import os; print(Pinecone(api_key=os.environ['PINECONE_API_KEY']).describe_index('agroexport-granos').host)"
# o desde el dashboard: app.pinecone.io -> tu índice -> copiar el "Host"
# Tiene el formato: agroexport-granos-xxxxxxx.svc.aped-xxxx-xxxx.pinecone.io
PINECONE_HOST=

# Opcionales (tienen default en api/chat.js si no se definen)
PINECONE_INDEX=agroexport-granos
PINECONE_NAMESPACE=default
